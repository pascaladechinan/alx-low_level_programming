#include "main.h"
/**
 * _strlen - returns length of a string
 * @s: string to find length
 * Return: length of a string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	continue;
	}
	return (i);
}
